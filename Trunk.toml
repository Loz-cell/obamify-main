[build]
# Hash the generated JavaScript and WebAssembly so a new deployment cannot be
# mixed with stale browser/CDN assets. The stable worker.js entry point receives
# the generated module URL from the application at runtime.
filehash = true
